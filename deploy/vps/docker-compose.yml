services:
  postgres:
    image: postgis/postgis:16-3.4
    container_name: geoapi-test-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - geoapi_test_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 10
    networks:
      - geoapi_test_internal

  api:
    build:
      context: ../..
      dockerfile: apps/api/Dockerfile
    container_name: geoapi-test-api
    restart: unless-stopped
    env_file:
      - .env.production
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "127.0.0.1:18110:8000"
    networks:
      - geoapi_test_internal

volumes:
  geoapi_test_postgres_data:

networks:
  geoapi_test_internal:
